{
  "name": "integ-games",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-build-version.mjs flyfy1/integ-games games.integ.life public/build-version.json",
    "build": "tsc -b && vite build && node scripts/generate-share-pages.mjs dist",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:lighthouse": "lhci autorun",
    "capture:art": "node scripts/capture-game-art.mjs",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vite-plugin-pwa": "^1.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@lhci/cli": "^0.15.1",
    "@types/node": "^24.0.0",
    "jsdom": "^26.0.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
